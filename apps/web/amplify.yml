version: 1
applications:
  - appRoot: apps/wb
    frontend:
      phases:
        preBuild:
          commands:
            - yarn install --frozen-lockfile
        build:
          commands:
            - npx nx export:browser web
        artifacts:
          baseDirectory: /
          files:
            - dist
        cache:
          paths:
            - node_modules/**/*